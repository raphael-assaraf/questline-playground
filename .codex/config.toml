# The cloud loop needs the network for git, gh, and app.questline.ai.
# No local Chrome and no recorder run on this machine; ~/.qa-agent
# holds the login token when the CLI path is used.
sandbox_mode = "workspace-write"

[sandbox_workspace_write]
network_access = true
writable_roots = ["~/.qa-agent"]
